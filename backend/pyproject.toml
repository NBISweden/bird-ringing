[project]
name = "bird_ringing"
version = "0.1.0"
description = "The bird ringing project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.7",
    "gunicorn>=23.0.0",
]
