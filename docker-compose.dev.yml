services:

  frontend:
    build:
      target: dev
    volumes:
      - ./frontend:/app
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://frontend:3000"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  backend:
    build:
      target: dev
    volumes:
      - ./backend:/app

  proxy:
    environment:
      SERVICE_MODE: development
    depends_on:
      frontend:
        condition: service_healthy

